from setuptools import find_packages, setup

setup(
    name='rules_pricing_engine',
    packages=find_packages(),
    version='0.1.0',
    description='A Python library that provides the rules and pricing for deriving a premium for an insurance.',
    author='Me',
)